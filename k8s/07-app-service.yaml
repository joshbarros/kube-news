apiVersion: v1
kind: Service
metadata:
  name: kube-news-api-service
  namespace: kube-news
  labels:
    app: kube-news-api
spec:
  selector:
    app: kube-news-api
  ports:
    - port: 80
      targetPort: 3001
      nodePort: 30081
  type: NodePort
